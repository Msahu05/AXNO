# MongoDB Database Configuration
# ============================================
# Get your connection string from MongoDB Atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/axno

# JWT Secret Key
# ============================================
# Generate a random secret key (minimum 32 characters)
# You can use: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-to-a-random-string-minimum-32-characters

# Server Port
# ============================================
# Port where the backend server will run
PORT=3001

# Email Configuration (Brevo SMTP)
# ============================================
# Using Brevo (formerly Sendinblue) for email delivery
# STEP 1: Sign up at https://www.brevo.com/
# STEP 2: Go to Settings → SMTP & API → SMTP
# STEP 3: Copy "SMTP Login" (email address) - this is your EMAIL_USER
# STEP 4: Copy "SMTP Key" (password) - this is your EMAIL_PASS
# STEP 5: Go to Settings → Senders → Add a sender and verify it
# STEP 6: Use verified sender email as FROM_EMAIL
# Note: SMTP Login is NOT your Brevo account email, it's a special SMTP login email shown in dashboard
EMAIL_USER=your-smtp-login-email@smtp-relay.brevo.com
EMAIL_PASS=your-brevo-smtp-key-here

# Brevo SMTP Configuration
# ============================================
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587

# From Email Address (Verified Sender)
# ============================================
# IMPORTANT: This must be a VERIFIED sender email in Brevo
# Go to Brevo Dashboard → Settings → Senders → Add and verify sender
# Use the verified email address here (can be different from EMAIL_USER)
# If not set, EMAIL_USER will be used as fallback
FROM_EMAIL=your-verified-email@yourdomain.com

# Admin Secret
# ============================================
# Secret key for setting admin users (keep this secure!)
ADMIN_SECRET=set-admin-secret-in-production

# Node Environment
# ============================================
# Set to 'production' when deploying
NODE_ENV=development

# Payment Gateway Configuration
# ============================================
# Payment mode: 'test' for development (simulated payments), 'production' for real gateway
# In test mode, payments are simulated - no real money is charged
# Perfect for development and testing the complete order flow
PAYMENT_MODE=production
# Options: 'test' (default) or 'production'

# Razorpay Configuration
# ============================================
# Get your keys from: https://dashboard.razorpay.com/app/keys
# For test mode, use test keys from Razorpay dashboard
# For production, use live keys from Razorpay dashboard
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

